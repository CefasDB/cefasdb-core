{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cefas.serviceAccountName" . }}
  labels:
    {{- include "cefas.labels" . | nindent 4 }}
{{- end }}
